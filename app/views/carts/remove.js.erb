$('.remove_item').bind('ajax:success', function() {
        var quantity = +($(this).closest('tr').children(".qty").text());
        if (quantity == 1){
          $(this).closest('tr').fadeOut();
        }
        else{
          quantity--;
          $(this).closest('tr').children(".qty").text(quantity.toString());
        }
});
