<%= form_tag cart_items_url, :html => { :multipart => true } do |f| %>
  <%= f.hidden_field :dish_id %>
  <%= f.select_tag :amount, options_for_select((1..20).to_a) %>
  <%= f.submit "Add to Cart", :class => "button" %>
<% end %>
