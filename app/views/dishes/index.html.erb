<%= render 'layouts/navbar' %>
<p id="notice"><%= notice %></p>

<h1>Dishes</h1>

<div class="page">
  <div class="row">
    <div class="cols">
      <% @dishes.each do |dish| %>
        <%= render "dishes/dish_row", dish: dish, order_item: @order_item %>
      <% end %>
    </div>
  </div>
</div>
